#Git remote

- git remote -v 
- git remote rename origin training
- git push -u training main

# Visualize
- git log

# Branch
- git branch -M main
- git branch --list
- git branch name && git checkout name